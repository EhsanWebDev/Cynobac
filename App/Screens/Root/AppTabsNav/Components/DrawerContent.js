import React, {useState} from 'react';
import {SafeAreaView, View} from 'react-native';

function DrawerContent({navigation, authData, selectedServer}) {
  return <SafeAreaView></SafeAreaView>;
}

export default DrawerContent;
